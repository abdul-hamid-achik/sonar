version: 1
contractHash: sha256:4c9f0e9d1529017ba37ce36a8c4d89d314aee4a211c25451b6303e2ae1bf3e36
name: local_agent_tui_smoke
intent: |
    a user can launch the sonar TUI, see the initial screen, and quit with ctrl+c.
target:
    cmd: ["./bin/sonar"]
    cwd: "."
    env:
        HOME: "${projectRoot}/.glyphrun/tmp/home"
        SONAR_AGENTS_DIR: "${projectRoot}/.glyphrun/tmp/agents"
        OLLAMA_HOST: "http://127.0.0.1:1"
preconditions:
    commands:
        - run: "rm -rf .glyphrun/tmp/home .glyphrun/tmp/agents && mkdir -p .glyphrun/tmp/home/.config/sonar .glyphrun/tmp/agents && printf '%s\\n' 'model:' '  auto_select: false' 'ice:' '  enabled: false' > .glyphrun/tmp/home/.config/sonar/config.yaml && go build -o ./bin/sonar ./cmd/sonar"
          cwd: "."
          timeoutMs: 30000
steps:
    - wait:
        screen:
            contains: "DEEPSEEK_API_KEY"
        timeoutMs: 8000
    - snapshot: launch
    - press: "ctrl+c"
    - wait:
        process:
            exited: true
        timeoutMs: 8000
outcomes:
    - id: launch_snapshot_stable
      description: the normal-width welcome and the missing-credential guidance remain stable
      verify:
        snapshot:
            name: launch
            mode: text
    - id: welcome_visible
      description: launch names the resolved provider and model
      verify:
        screen:
            contains: "deepseek"
    - id: ctrl_c_quits
      description: ctrl+c exits the TUI cleanly
      verify:
        process:
            exitCode: 0
